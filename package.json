{
  "name": "agent-news",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src/",
    "cf-typegen": "wrangler types",
    "migrate": "npx tsx scripts/migrate-kv-to-do.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^25.3.3",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "@noble/hashes": "^2.0.1",
    "@noble/secp256k1": "^2.1.0",
    "@scure/base": "^2.0.0",
    "@scure/btc-signer": "^2.0.1",
    "hono": "^4.6.0"
  }
}
